# ── Supabase stack — pinned to a verified-consistent set (DO NOT loosen) ───────
# The old supabase 2.3.4 had internally-inconsistent transitive constraints: its
# sub-package called httpx(proxy=...) but constrained httpx too low to accept it,
# so a fresh CI install crashed with:
#   TypeError: Client.__init__() got an unexpected keyword argument 'proxy'
# Fix = upgrade to a modern, self-consistent release (gotrue→supabase-auth,
# supafunc→supabase-functions). Verified: this set installs cleanly and
# create_client() constructs without the proxy crash. Our code only uses the
# stable create_client / table().select/upsert/maybe_single API.
supabase==2.31.0
supabase-auth==2.31.0
supabase-functions==2.31.0
postgrest==2.31.0
storage3==2.31.0
realtime==2.31.0
httpx==0.28.1
websockets==15.0.1

# ── Scraping / HTTP ────────────────────────────────────────────────────────────
requests==2.31.0
beautifulsoup4==4.12.2
lxml==4.9.4

# ── Gmail (OAuth) ──────────────────────────────────────────────────────────────
google-auth==2.28.0
google-auth-oauthlib==1.2.0
google-api-python-client==2.119.0

# ── Misc ───────────────────────────────────────────────────────────────────────
resend==0.7.2
python-dotenv==1.0.0
