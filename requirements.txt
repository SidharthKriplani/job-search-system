# ── Supabase stack — pinned together (DO NOT loosen) ───────────────────────────
# supabase 2.3.4 leaves its sub-dependencies unpinned, so a fresh CI install can
# resolve a newer httpx/gotrue that crashes with:
#   TypeError: Client.__init__() got an unexpected keyword argument 'proxy'
# Pinning the whole transitive stack to a verified-good combo prevents that drift.
supabase==2.3.4
gotrue==2.9.1
postgrest==0.15.1
storage3==0.7.7
supafunc==0.3.3
realtime==1.0.6
httpx==0.25.2

# ── Scraping / HTTP ────────────────────────────────────────────────────────────
requests==2.31.0
beautifulsoup4==4.12.2
lxml==4.9.4

# ── Gmail (OAuth) ──────────────────────────────────────────────────────────────
google-auth==2.28.0
google-auth-oauthlib==1.2.0
google-api-python-client==2.119.0

# ── Misc ───────────────────────────────────────────────────────────────────────
resend==0.7.2
python-dotenv==1.0.0
