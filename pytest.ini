[pytest]
# Run from repo root: `pytest`. Adds the repo root to import path so
# `from utils.filter import ...` works without PYTHONPATH gymnastics.
pythonpath = .
testpaths = tests
python_files = test_*.py
addopts = -q
